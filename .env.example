# Exact browser origin(s), comma-separated. Production values must use HTTPS.
ALLOWED_ORIGINS=https://your-pages-project.pages.dev
APP_ENV=production
# Render's current default changes over time; deploy this tested version explicitly.
PYTHON_VERSION=3.12.6
# No Groq or Mistral key belongs in server configuration.
